#include <stdio.h>

/**
 * main - simple main function that prints msg to stdout
 * Return: 0 exit code
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
